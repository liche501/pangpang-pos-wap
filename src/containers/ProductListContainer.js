import React, { Component } from 'react';

export default class ProductListContainer extends Component {
    render() {
        return (
            <div>
                ProductList
            </div>
        );
    }
}
